<%= form_for(@exercises_performed) do |f| %>
  <% if @exercises_performed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exercises_performed.errors.count, "error") %> prohibited this Exercise Performed from being saved:</h2>

      <ul>
      <% @exercises_performed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <dv class="field">
    <%= f.label :exercise_type_id %>
    <%= f.select :exercise_type_id, options_from_collection_for_select(@types, :id, :name), prompt: "Select Exercise Type" %>
  </div>
  <div class="field">
    <%= f.label :calories_burned %><br>
    <%= f.number_field :calories_burned, class: :text_field, step: 0.01  %>
  </div>
  <div class="field">
    <%= f.label :date_burned %><br>
    <%= f.date_select :date_burned %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
