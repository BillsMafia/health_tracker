<%= form_for(@exercises_performed) do |f| %>
  <% if @exercises_performed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exercises_performed.errors.count, "error") %> prohibited this steps_taken from being saved:</h2>

      <ul>
      <% @exercises_performed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <dv class="field">
    <%= f.label :exercise_type_id %>
    <%= f.select(:exercise_type_id) %>
  </div>
  <div class="field">
    <%= f.label :calories_burned %><br>
    <%= f.number_field :calories_burned %>
  </div>
  <div class="field">
    <%= f.label :date_burned %><br>
    <%= f.date_select :date_burned %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
