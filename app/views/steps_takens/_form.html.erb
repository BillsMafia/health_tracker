<%= form_for(@steps_taken) do |f| %>
<%= blow up %>
  <% if @steps_taken.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@steps_taken.errors.count, "error") %> prohibited this steps_taken from being saved:</h2>

      <ul>
      <% @steps_taken.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :steps_taken %><br>
    <%= f.number_field :steps_taken %>
  </div>
  <div class="field">
    <%= f.label :date_walked %><br>
    <%= f.date_select :date_walked %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
